import express from "express";
// import {faultfilter, faultfind, faultall, faultadd} from "../controllers/fault.js";


const route = express.Router();


// //for filtering by query
// fault.get("/createPost/", faultfilter);
// //for get data related to an id
// fault.get("/filter/id/:faultid", faultfind);
// //fot getting all faults
// fault.get("/filter/all", faultall);
// //for adding new faults
// fault.post("/add", faultadd);
    


export default route;
